import { NavbarBacklightDirective } from './navbar-backlight.directive';

describe('NavbarBacklightDirective', () => {
  it('should create an instance', () => {
    const directive = new NavbarBacklightDirective();
    expect(directive).toBeTruthy();
  });
});
