import { ButtonAnimationDirective } from './button-animation.directive';

describe('ButtonAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
