import { AppNeedsDirective } from './app-needs.directive';

describe('AppNeedsDirective', () => {
  it('should create an instance', () => {
    const directive = new AppNeedsDirective();
    expect(directive).toBeTruthy();
  });
});
