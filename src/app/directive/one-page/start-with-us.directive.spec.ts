import { StartWithUsDirective } from './start-with-us.directive';

describe('StartWithUsDirective', () => {
  it('should create an instance', () => {
    const directive = new StartWithUsDirective();
    expect(directive).toBeTruthy();
  });
});
