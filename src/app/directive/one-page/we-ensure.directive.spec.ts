import { WeEnsureDirective } from './we-ensure.directive';

describe('WeEnsureDirective', () => {
  it('should create an instance', () => {
    const directive = new WeEnsureDirective();
    expect(directive).toBeTruthy();
  });
});
