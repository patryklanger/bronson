import { FeelingDirective } from './feeling.directive';

describe('FeelingDirective', () => {
  it('should create an instance', () => {
    const directive = new FeelingDirective();
    expect(directive).toBeTruthy();
  });
});
