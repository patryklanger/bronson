import { MainPageDirective } from './main-page.directive';

describe('MainPageDirective', () => {
  it('should create an instance', () => {
    const directive = new MainPageDirective();
    expect(directive).toBeTruthy();
  });
});
